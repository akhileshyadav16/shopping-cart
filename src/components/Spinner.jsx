import "./Spinner.css";

export default function Spinner(){
    return(
        <div className="text-black h-screen flex w-screen items-center justify-center">
           <span class="loader"></span> 
        </div>
    );
}